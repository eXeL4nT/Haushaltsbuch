<UserControl x:Class="HouseholdBook.WPF.Views.EditBookingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HouseholdBook.WPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel>
        <Label Content="Label" />
        <TextBox x:Name="tbTitle" Text="{Binding Path=Title}"/>
        <TextBox x:Name="tbAmount" Text="{Binding Path=Amount}"/>
        <DatePicker x:Name="dpDate" Text="{Binding Path=Date}"/>
        <!--<ComboBox x:Name="Category" Text="{Binding Path=Category.Title}"/>-->
        <!--<ComboBox x:Name="BankAccount" Text="{Binding Path=BankAccount.Name}"/>-->
        <!--<Button x:Name="Save" Content="Speichern" Click="Save_Click"/>-->
    </StackPanel>
    
</UserControl>
